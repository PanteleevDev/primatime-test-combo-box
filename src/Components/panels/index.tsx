import { lazy } from "react";

export const NativePanel = lazy(() => import('./native-panel/native-panel'));
export const ReactHookFormPanel = lazy(() => import('./react-hook-form-panel/react-hook-form-panel'));